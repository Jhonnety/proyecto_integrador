streamlit>=1.31.0
# El pipeline serializado es un imblearn.pipeline.Pipeline, por lo que
# imbalanced-learn es necesario para deserializarlo aunque el modelo final
# no incorpore ningun paso de remuestreo.
imbalanced-learn>=0.14.0
# Piso en la version que serializo el modelo: las versiones anteriores de
# scikit-learn no pueden leer un pickle generado por una posterior.
scikit-learn>=1.9.0
pandas>=2.0.0
numpy>=1.24.0
joblib>=1.3.0
